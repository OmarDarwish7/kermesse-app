import * as mongoose from 'mongoose';
export declare const testSchema: mongoose.Schema<any, mongoose.Model<any, any, any, any>, any, any>;
export interface Test {
    _id: string;
    Magasin: string;
}
